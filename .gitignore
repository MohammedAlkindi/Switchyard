# Fleet's own runtime state must never be committed to any repo it manages —
# including this one.
.fleet/

node_modules/
dist/
coverage/
*.tsbuildinfo

# Per-developer Claude Code overrides; the shared .claude/settings.json IS committed.
.claude/settings.local.json

.env
.DS_Store
npm-debug.log*
